<Window x:Class="FileTinder.Views.MtpFolderInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Source Folder"
        Width="500" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1a1a2e"
        Foreground="White"
        FontFamily="Segoe UI">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter the MTP path to scan for sub-folders:"
                   FontSize="13" Margin="0,0,0,8"/>

        <TextBox Grid.Row="1" x:Name="PathBox"
                 FontSize="13" Padding="8,6"
                 Background="#333" Foreground="White" BorderBrush="#555"
                 CaretBrush="White"
                 Margin="0,0,0,6"/>

        <TextBlock Grid.Row="2"
                   Text="Example: \Internal Storage"
                   FontSize="11" Foreground="#888" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click"
                    Background="#333" Foreground="White"
                    BorderThickness="0" Padding="14,7" Margin="0,0,10,0"
                    Cursor="Hand"/>
            <Button Content="OK" Click="OK_Click"
                    Background="#4ade80" Foreground="#1a1a2e"
                    FontWeight="Bold"
                    BorderThickness="0" Padding="20,7"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
